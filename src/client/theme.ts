// theme.ts
import { createTheme } from "@material-ui/core/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#ff0000", // Color rojo para la AppBar y otros elementos primarios
    },
  },
});

export default theme;
